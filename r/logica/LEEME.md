# Zona de pegado — Acciones PIN

Un solo `.R` en esta carpeta (auto-descubierto por `r/main.R`).

Actual: `acciones_pin.R` (pristine). Entrada: `Acciones` / `Documentos` / `AdminUF`
desde `STG_ORA_*`. Salida: `Acciones_Ubigeo`.

No abrir conexiones ni `library()` aqui. Ver `r/CONTRATO.md`.
